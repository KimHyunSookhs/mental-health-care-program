import 'package:flutter/material.dart';

class SecretStroeScreen extends StatelessWidget {
  const SecretStroeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Text('마음창고'),
    ));
  }
}
