import 'package:flutter/material.dart';

class HaruCard extends StatelessWidget {
  const HaruCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
